spring:
  datasource:
    url: jdbc:h2:mem:purchase-cart-service
    driver-class-name: org.h2.Driver
    username: admin
    password:
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console